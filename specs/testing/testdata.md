---
size: small
modified_date: 2026-04-26
implemented_git_tag: specs/testing/testdata/implemented
---

# Create test data

Create fake data for every data element the code is using. Use separate files for separate data elements (the mechanism to connect them in a single dictionary is already in the code)

Create a fake picture for photo.

Put fake data in /testdata/yaml.

Create multiple configuration `.toml` files testing
- Single page CV
- Two page CV
- Tweaking exposed parameters in /j2/css/tokens.css.j2


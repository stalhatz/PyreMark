---
size: small
---

Add a qr code template. 

**Goal**: provide a way for someone that receive a CV in physical form to access the digitalised format

It should be related to qr code data that are essentially just a link to the site it points to as well as an optional header text to put over the qr code. It should be either 
- placed as the last template to take either all the remaining space or a sepearated page all by itself or
- placed as a free floating element wherever there is any whitespace on the CV (that is going to be really hard to do)

# Technical 
The `qrcode` python package should be well suited for this job